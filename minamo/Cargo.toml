[package]
name = "minamo"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
nix = { version = "0.31.3", features = ["fs", "ioctl", "poll", "signal", "term"] }
xxhash-rust = { version = "0.8.18", features = ["xxh3"] }

[profile.release]
lto = true
codegen-units = 1
strip = true
